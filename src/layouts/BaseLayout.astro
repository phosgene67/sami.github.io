---
import '../styles/global.css'
import Header from '../components/Header.astro'
import Footer from '../components/Footer.astro'
import MetaTags from '../components/MetaTags.astro'
import CookieBanner from '../components/CookieBanner.astro'

interface Props {
  title: string
  description?: string
  image?: string
  canonicalURL?: string
  isHomepage?: boolean
  article?: {
    publishedTime: string
    tags: string[]
  }
}

const {
  title,
  description = 'Sami Bashraheel — Web & AI Developer. Building digital products and experiments with modern web technologies.',
  image = '/og-image.jpg',
  canonicalURL = Astro.url.href,
  isHomepage = false,
  article,
} = Astro.props
---

<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="google-site-verification" content="0V0322chyCByP8rbrMp5mlD1nHcx7I4JnuyNrqxBfTs" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg?v=2" />
    <link rel="alternate icon" type="image/png" href="/favicon.png?v=2" />
    <link rel="apple-touch-icon" href="/favicon.png?v=2" />

    <MetaTags
      title={title}
      description={description}
      image={image}
      canonicalURL={canonicalURL}
      isHomepage={isHomepage}
      article={article}
    />

    <!-- Theme logic handled by ThemeToggle.astro -->
    <script is:inline>
      ;(function () {
        const stored = localStorage.getItem('theme')
        let theme = stored
        if (!stored) {
          if (
            window.matchMedia &&
            window.matchMedia('(prefers-color-scheme: dark)').matches
          ) {
            theme = 'midnight'
          } else {
            theme = 'mint'
          }
        }

        if (theme !== 'midnight') {
          document.documentElement.classList.add(`theme-${theme}`)
        }
      })()
    </script>

    <!-- Google Analytics -->
    {
      import.meta.env.PUBLIC_GA_ID && (
        <>
          <script
            async
            src={`https://www.googletagmanager.com/gtag/js?id=${import.meta.env.PUBLIC_GA_ID}`}
          />
          <script is:inline define:vars={{ gaId: import.meta.env.PUBLIC_GA_ID }}>
            window.dataLayer = window.dataLayer || []
            function gtag() {
              dataLayer.push(arguments)
            }
            gtag('js', new Date())

            // Default to denied
            gtag('consent', 'default', {
              analytics_storage: 'denied',
            })

            // Check if user already granted consent
            const consent = localStorage.getItem('cookieConsent')
            if (consent === 'granted') {
              gtag('consent', 'update', {
                analytics_storage: 'granted',
              })
            }

            gtag('config', gaId)
          </script>
        </>
      )
    }

    <!-- Vercel Analytics -->
    <script is:inline src="/_vercel/insights/script.js" defer></script>
  </head>
  <body
    class="font-sans antialiased flex flex-col min-h-screen px-6 md:px-12 max-w-4xl mx-auto"
  >
    <a href="#main-content" class="skip-link">Skip to main content</a>
    <Header />
    <main
      id="main-content"
      class="flex-grow flex flex-col py-10 space-y-12 w-full"
    >
      <slot />
    </main>
    <Footer />
    <CookieBanner />
  </body>
</html>
